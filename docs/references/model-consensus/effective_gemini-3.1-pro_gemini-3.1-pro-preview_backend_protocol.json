{
  "ok": true,
  "status": 200,
  "actual_model": "gemini-3.1-pro-preview",
  "tag": "backend_protocol",
  "content": "作为资深架构师和产品评审，我直接切入核心。你的约束非常清晰（**不自研执行器、MCP仅做登记/状态机**），这决定了 Science Mode 的本质是一个**“科研生命周期状态机 + 溯源图谱记录仪”**，而不是一个 Jupyter Kernel。\n\n以下是具体、可执行的后端协议与架构设计建议：\n\n---\n\n### 一、 Schema 设计 (`common/chat/scienceMode.ts`)\n\n不要把 Science Mode 当作普通的对话，把它定义为一个**有向无环图 (DAG)** 的构建过程。\n\n```typescript\n// 核心：科研产物与溯源 Schema\nexport interface ScienceArtifact {\n  id: string; // UUID\n  type: 'dataset' | 'code' | 'figure' | 'manuscript' | 'report';\n  uri: string; // 本地 file:// 或 远程 ssh:// 路径，绝不存实体\n  hash: string; // SHA-256，保证可复现性防篡改\n  metadata: Record<string, any>; // 例如：{ format: 'csv', rows: 1000 }\n}\n\nexport interface ProvenanceRecord {\n  id: string;\n  action: 'data_cleaning' | 'analysis' | 'visualization' | 'drafting';\n  inputs: string[]; // 依赖的 Artifact IDs (复用 PaperClip 检索到的文献也作为 input)\n  executor: 'local_python' | 'slurm_hpc' | 'human'; // 调用的现有 runner\n  code_snapshot: string; // 执行时的代码快照或脚本路径\n  outputs: string[]; // 生成的 Artifact IDs\n  timestamp: number;\n}\n\n// 注入给 LLM 的 Context Schema\nexport interface ScienceProjectState {\n  projectId: string;\n  currentPhase: 'hypothesis' | 'experiment' | 'analysis' | 'writing' | 'review';\n  artifacts: ScienceArtifact[];\n  provenanceGraph: ProvenanceRecord[];\n}\n```\n\n### 二、 MCP Tools 设计 (`scienceServer.ts`)\n\n**严格遵守约束**：Science MCP 不执行代码，只做“登记”和“状态流转”。LLM 需要组合使用 `Existing Runner MCP` (执行) + `Science MCP` (登记)。\n\n```typescript\n// Science MCP 提供的 Tools\nconst scienceTools = [\n  {\n    name: \"register_artifact\",\n    description: \"当通过 runner 生成了新文件（图表/数据/文档）后，必须调用此工具登记产物\",\n    parameters: { /* type, uri, description */ }\n  },\n  {\n    name: \"record_provenance\",\n    description: \"记录 E1/E2 溯源关系。声明哪个脚本处理了哪些输入，生成了哪些输出\",\n    parameters: { /* inputs[], code_used, outputs[] */ }\n  },\n  {\n    name: \"request_privacy_approval\",\n    description: \"当需要处理敏感医学/科研数据，或向外部 HPC 提交任务前，发起审批\",\n    parameters: { /* data_uri, intended_action, justification */ }\n  },\n  {\n    name: \"submit_to_reviewer\",\n    description: \"将当前 manuscript 和 figures 提交给 Reviewer Agent 进行图文稿迭代\",\n    parameters: { /* manuscript_id, figure_ids[] */ }\n  }\n];\n```\n*工作流示例*：LLM 决定画图 -> 调用 `python_runner` (现有) 执行代码生成 `plot.png` -> LLM 调用 `register_artifact` 登记 `plot.png` -> LLM 调用 `record_provenance` 绑定代码与图片。\n\n### 三、 与医学循证 (Medical Evidence) 的迁移与平行架构\n\n**痛点**：现在的 `useGuidSend.ts` 和 `MessageList` 可能是 hardcode 了医学模式的逻辑。\n**重构方案**：引入 **Strategy Pattern (策略模式)**，解耦 Mode 与 Core Chat。\n\n1.  **抽象 ModeProvider**：\n    定义 `IModeProvider` 接口，包含 `getSystemPrompt()`, `getMCPTools()`, `getExtraContext()`, `parsePanelData()`。\n2.  **改造 `useGuidSend.ts`**：\n    ```typescript\n    // 伪代码：消除 if (mode === 'medical') 的坏味道\n    const modeProvider = ModeFactory.getProvider(currentMode); // 'medical' | 'science'\n    const prompt = modeProvider.getSystemPrompt(projectState);\n    const tools = [...baseTools, ...modeProvider.getMCPTools()];\n    // 发送请求...\n    ```\n3.  **改造 `MessageList` 与 Panel 渲染**：\n    现有的 `MedicalEvidencePanel` 是根据 tool output 渲染的。新增 `ScienceProvenancePanel`。\n    在 `MessageList` 中使用注册表模式：\n    ```tsx\n    const PanelRegistry = {\n      'medicalEvidence': MedicalEvidencePanel,\n      'scienceArtifact': ScienceArtifactViewer, // 原生科学 viewer，复用现有的 Preview 组件\n    };\n    // 渲染时：<Panel Component={PanelRegistry[message.toolName]} data={message.toolData} />\n    ```\n\n### 四、 持久化边界 (Persistence Boundaries)\n\n必须严格分离“元数据”与“物理文件”，否则 SQLite 会被撑爆，且无法与 HPC 协同。\n\n*   **Agent DB (SQLite/IndexedDB)**：只存 `ScienceProjectState` (包含 Schema 中的 `ScienceArtifact` 记录和 `ProvenanceRecord` 关系图)。\n*   **Workspace (本地文件系统 / 远程挂载)**：存真实的 Python 脚本、CSV、PDF、图片。\n*   **边界规则**：Agent DB 中的 `uri` 是唯一凭证。如果用户在外部删除了文件，Agent 在加载 `Preview` 时应捕获 `File Not Found` 并提示 LLM 重新生成（触发复现流程）。\n\n### 五、 权限边界 (Permission Boundaries)\n\n针对隐私审批和 SSH/Slurm/Modal 远程执行：\n\n1.  **拦截器模式 (Interceptor)**：在 `Existing Runner` 和 `Science MCP` 之间建立权限网关。\n2.  **状态机挂起 (Suspense)**：\n    *   当 LLM 试图通过 runner 访问标记为 `sensitive` 的数据集，或调用 `ssh_runner` 时。\n    *   后端拦截请求，向前端推送 `STATUS: PENDING_APPROVAL`。\n    *   前端渲染审批卡片（Approve / Reject / Modify）。\n    *   审批通过后，携带 `approval_token` 恢复执行。\n\n---\n\n### 六、 产品评审：最大矛盾与风险\n\n**最大矛盾：“不自研执行器” 与 “可复现 artifacts (E1/E2 provenance)” 之间的巨大张力。**\n\n*   **风险点**：现有的 runner（如普通的 bash/python MCP）是**“黑盒”**且**“有副作用”**的。LLM 可能会写一段代码覆盖了旧的数据，或者在代码里硬编码了绝对路径。因为你只靠 LLM 自觉去调用 `register_artifact` 和 `record_provenance`，**LLM 极大概率会漏记、错记、或者产生幻觉**（比如代码跑失败了，但它登记了产物）。\n*   **架构解法（必须做）**：\n    1.  **强制沙盒化 (Workspace Isolation)**：每次执行，Runner 必须在一个独立的子目录（或 Git commit 节点）中运行。\n    2.  **被动监听兜底**：不能全指望 LLM 主动登记。Agent 后端需要监听 Workspace 的文件系统变化（File System Watcher）。如果 Runner 执行完后，目录里多出了 `result.csv`，但 LLM 没有调用 `register_artifact`，系统应强制注入一条 System Prompt：“检测到新文件 result.csv，请调用 register_artifact 登记并说明溯源”。\n\n### 七、 M1 最小闭环 (Minimum Viable Loop)\n\n不要一上来就搞 Slurm 和 Reviewer，M1 只做**本地单机科研闭环**。\n\n**M1 目标**：跑通“假设 -> 数据检索 -> 代码执行 -> 产物登记 -> 图文渲染”的单线流程。\n\n**M1 执行步骤**：\n1.  **UI/入口**：新建 \"Science Project\"，初始化 `projectId` 和空 Workspace 目录。\n2.  **检索 (复用)**：LLM 调用现有的 `PaperClip` 检索文献，调用 `user_input` 获取用户指令。\n3.  **执行 (复用)**：LLM 编写 Python 数据分析代码，调用现有的 `local_python_runner` 执行。\n4.  **登记 (新增)**：LLM 调用 `register_artifact` 登记生成的图表。\n5.  **渲染 (复用+微调)**：`MessageList` 监听到登记事件，调用现有的 `Preview` 组件，在右侧 Panel 渲染 Markdown 报告和生成的图片。\n6.  **溯源 (新增)**：在 Panel 底部，渲染一个简单的 E1/E2 溯源链条（Input: 文献A -> Code: script.py -> Output: figure.png）。\n\n**M1 暂不做的**：SSH/Slurm 远程执行、复杂的 Reviewer 多 Agent 辩论、严格的隐私审批流（M1 默认本地数据全信任）。",
  "finish_reason": "stop",
  "raw_id": "chatcmpl-202607010710408431083708268d9d6RWE6W4QG",
  "requested_model": "gemini-3.1-pro",
  "base_url": "https://api.bianxie.ai/v1",
  "file": "effective_gemini-3.1-pro_gemini-3.1-pro-preview_backend_protocol.json"
}